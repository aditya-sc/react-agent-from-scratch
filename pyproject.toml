[project]
name = "react-agent-from-scratch"
version = "0.1.0"
description = "A ReAct agent built from scratch with zero frameworks (raw model API only)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.31",
]
